<div class="profile deep-orange lighten-2 z-depth-1">
  <h1 class="grey-text text-lighten-5">Jam Sessions</h1>
</div>

<div class="row">
  <div class= "col 12">
    <div class= "container" >
      <div class= "col s6">
          <%= form_tag(jam_sessions_path, method: "get") do %>
            <%= text_field_tag(:search_city, params[:search_city], placeholder: "Search by City:") %>
                <a class="waves-effect waves-light btn"><%= submit_tag("search", name: nil) %></a>
            <% end %>
        </div>
        <div class= "col s6">
          <%= form_tag(jam_sessions_path, method: "get") do %>
            <%= text_field_tag(:search_genre, params[:search_genre], placeholder: "Search by Genre:") %>
              <a class="waves-effect waves-light btn"><%= submit_tag("search", name: nil) %></a>
          <% end %>
      </div>
    </div>
  </div>
</div>

<div class="playing_jams deep-orange lighten-2 z-depth-1">
  <h3 class="grey-text text-lighten-5">Create A Jam Session!</h3>
</div>
<%= link_to 'NEW SESH', new_jam_session_path %>

<div class="playing_jams deep-orange lighten-2 z-depth-1">
  <h3 class="grey-text text-lighten-5">All Jam Sessions</h3>
</div>
<h4 class="teal-text">Click link to sign up!</h4>

  <ul>
  <% @jamsessions.each do |js| %>
    <div class="row">
        <div class="col s12 m5">
            <div class="card-panel white">
                <h4 class="white-text"><%=link_to js.title, jam_session_path(js)%></h4><br />
                <h5 class="bold-text">Jam Description: </h5>
                <p> <%= js.description %> </p>
              </div>
            </div>
          </div>
    <% end %>
  </ul>
